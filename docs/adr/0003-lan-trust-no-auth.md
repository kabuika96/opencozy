# LAN Trust Without Authentication

OpenCozy does not authenticate clients in the first version. Any device that can reach the backend over the LAN can start or resume Codex as the local OS user, which is acceptable only because this project is intentionally scoped to a trusted private network.
