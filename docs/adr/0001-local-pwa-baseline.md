# Local PWA Baseline

OpenCozy is a locally hosted PWA with a React frontend, a Node/Fastify backend, SQLite under `./data`, and a Vite dev server reachable on the LAN. This keeps the project usable from an iPhone without introducing cloud hosting, accounts, or deployment machinery before the remote Codex workflow works.
